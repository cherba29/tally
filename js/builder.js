goog.provide('budget.Builder');


goog.scope(function() {

/**
 * @final
 * @constructor
 */
budget.Builder = function() {
};
var Builder = budget.Builder;


Builder.prototype.build = function(jsonData) {
//  var dataView = transformBudgetData(
//        jsonData.months, jsonData.accountNameToAccount, jsonData.statements, jsonData.summaries);
};


});  // goog.scope

goog.exportSymbol('budget.Builder', budget.Builder);
